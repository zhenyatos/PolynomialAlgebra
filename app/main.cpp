#include "Integer.hpp"
#include <iostream>
#include <exception>
#include <limits>

int main() {
    Integer a = INT_MAX;
    Integer b = INT_MIN;
    Integer c = norm(b + 1);
    std::cout << (Integer)1;
    return 0;
}